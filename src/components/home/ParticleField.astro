<div class="field-stage" data-particle-field aria-hidden="true">
  <canvas class="field-canvas" data-field-canvas aria-hidden="true"></canvas>
  <div class="field-halo"></div>

  <div class="field-hud">
    <div class="field-hud__head">
      <b>Resonant field</b>
      <span data-field-status>stable</span>
    </div>
    <div class="field-hud__row">
      <span>carrier</span>
      <code data-field-frequency>1.000 MHz</code>
    </div>
    <div class="field-hud__row">
      <span>phase orbit</span>
      <code data-field-phase>+37.2°</code>
    </div>
    <div class="field-hud__row">
      <span>field density</span>
      <code data-field-density>0.86</code>
    </div>
    <div class="field-hud__bars">
      <i style="--n: 5"></i><i style="--n: 10"></i><i style="--n: 7"></i><i style="--n: 14"></i
      ><i style="--n: 9"></i><i style="--n: 16"></i><i style="--n: 12"></i><i style="--n: 6"></i
      ><i style="--n: 13"></i><i style="--n: 8"></i><i style="--n: 15"></i><i style="--n: 11"></i>
    </div>
  </div>
</div>

<script>
  import '../../scripts/particle-field';
</script>

<style>
  .field-stage {
    position: relative;
    min-height: 600px;
    display: grid;
    place-items: center;
    pointer-events: none;
  }

  .field-stage::before {
    position: absolute;
    z-index: 0;
    inset: 4% -6% 5% -4%;
    background:
      radial-gradient(circle at 18% 22%, rgb(77 94 122 / 0.5) 0 1.5px, transparent 2.5px),
      radial-gradient(circle at 34% 36%, rgb(105 96 132 / 0.42) 0 1.8px, transparent 2.8px),
      radial-gradient(circle at 48% 19%, rgb(92 119 122 / 0.45) 0 1.4px, transparent 2.5px),
      radial-gradient(circle at 63% 42%, rgb(77 94 122 / 0.44) 0 1.6px, transparent 2.7px),
      radial-gradient(circle at 72% 27%, rgb(105 96 132 / 0.48) 0 1.9px, transparent 3px),
      radial-gradient(circle at 82% 51%, rgb(92 119 122 / 0.42) 0 1.4px, transparent 2.5px),
      radial-gradient(circle at 26% 61%, rgb(77 94 122 / 0.32) 0 1.2px, transparent 2.3px),
      radial-gradient(circle at 56% 69%, rgb(105 96 132 / 0.35) 0 1.3px, transparent 2.5px),
      linear-gradient(28deg, transparent 36%, rgb(82 98 128 / 0.11) 36.2% 36.35%, transparent 36.6%),
      linear-gradient(154deg, transparent 43%, rgb(82 98 128 / 0.1) 43.2% 43.35%, transparent 43.7%);
    content: "";
    mask-image: radial-gradient(ellipse at 58% 46%, black 0 56%, transparent 74%);
    opacity: 0.16;
    pointer-events: none;
  }

  .field-canvas {
    position: fixed;
    z-index: 0;
    inset: 0;
    width: 100vw;
    height: 100vh;
    max-width: none;
    pointer-events: none;
  }

  .field-halo {
    position: relative;
    z-index: 1;
    width: min(42vw, 560px);
    aspect-ratio: 1;
    border-radius: 50%;
    background:
      radial-gradient(
        circle at 46% 40%,
        rgb(255 255 255 / 0.8) 0 9%,
        rgb(255 255 255 / 0.16) 36%,
        transparent 64%
      ),
      radial-gradient(circle at 60% 56%, rgb(116 134 157 / 0.15), transparent 48%);
    box-shadow:
      inset 0 0 0 1px rgb(84 100 124 / 0.08),
      0 0 120px rgb(109 126 154 / 0.1);
    filter: blur(1px);
    opacity: 0.82;
  }

  .field-hud {
    position: absolute;
    z-index: 2;
    right: 2%;
    bottom: 14%;
    width: min(260px, 78%);
    padding: 17px 18px 15px;
    border: 1px solid rgb(67 83 105 / 0.18);
    border-radius: var(--radius-hud, 16px);
    color: var(--ink-soft, #525c6c);
    background: var(--surface-hud, rgb(247 248 245 / 0.58));
    box-shadow: var(--shadow-float, 0 22px 70px rgb(34 48 66 / 0.1));
    backdrop-filter: blur(18px) saturate(120%);
    -webkit-backdrop-filter: blur(18px) saturate(120%);
  }

  .field-hud__head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 10px;
    padding-bottom: 10px;
    border-bottom: 1px solid var(--line, rgb(51 65 86 / 0.16));
    font-size: 9px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .field-hud__head b {
    color: var(--ink, #18202c);
    font-weight: 650;
  }

  .field-hud__row {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 12px;
    align-items: center;
    margin-top: 7px;
    font-size: 11px;
  }

  .field-hud__row code {
    color: var(--ink, #18202c);
    font-family: var(--font-mono, ui-monospace, SFMono-Regular, Menlo, Consolas, monospace);
    font-size: 11px;
    font-variant-numeric: tabular-nums;
  }

  .field-hud__bars {
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    gap: 3px;
    align-items: end;
    height: 18px;
    margin-top: 13px;
  }

  .field-hud__bars i {
    display: block;
    height: min(calc((var(--n) + 2) * 1px), 18px);
    border-radius: 2px;
    background: linear-gradient(to top, rgb(89 104 132 / 0.2), rgb(89 104 132 / 0.72));
    transform-origin: bottom;
    animation: field-bar 2.7s ease-in-out infinite alternate;
    animation-delay: calc(var(--n) * -110ms);
  }

  @keyframes field-bar {
    to {
      transform: scaleY(0.44);
      opacity: 0.55;
    }
  }

  @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
    .field-hud {
      background: rgb(247 248 245 / 0.92);
    }
  }

  @media (max-width: 980px) {
    .field-stage {
      min-height: 440px;
      margin-top: -60px;
    }

    .field-halo {
      width: min(76vw, 560px);
    }

    .field-hud {
      right: 6%;
      bottom: 8%;
    }
  }

  @media (max-width: 720px) {
    .field-stage {
      min-height: 370px;
      margin-top: -34px;
    }

    .field-hud {
      right: 0;
      bottom: 2%;
      width: 220px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .field-stage::before {
      opacity: 0.82;
    }

    .field-hud__bars i {
      animation: none;
    }
  }
</style>
